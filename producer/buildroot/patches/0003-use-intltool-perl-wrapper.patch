--- a/package/Makefile.in
+++ b/package/Makefile.in
@@ -280 +280,2 @@ TARGET_MAKE_ENV = \
-	PATH=$(BR_PATH)
+	PATH=$(BR_PATH) \
+	LD_LIBRARY_PATH="$(HOST_DIR)/lib$${LD_LIBRARY_PATH:+:$${LD_LIBRARY_PATH}}"
@@ -330 +331,2 @@ HOST_MAKE_ENV = \
-	PKG_CONFIG_LIBDIR="$(HOST_DIR)/lib/pkgconfig:$(HOST_DIR)/share/pkgconfig"
+	PKG_CONFIG_LIBDIR="$(HOST_DIR)/lib/pkgconfig:$(HOST_DIR)/share/pkgconfig" \
+	LD_LIBRARY_PATH="$(HOST_DIR)/lib$${LD_LIBRARY_PATH:+:$${LD_LIBRARY_PATH}}"
